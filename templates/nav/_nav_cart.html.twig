{# CART NAV #}
<div id="dropdown-cart" class="modal modal-fixed-footer">
    <div class="modal-content cart-modal-content">
    </div>
    <div class="modal-footer" id="modal-footer-cart">
        <a class="modal-close waves-effect waves-green btn-flat not-anim">Continuer</a>
        <a class="waves-effect waves-green btn-flat" id="cart-validate" {% if app.user %}data-user="{{ app.user.id }}"
            {% endif %}>Commander</a>
    </div>
</div>