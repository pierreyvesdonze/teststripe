{% extends 'base.html.twig' %}

{% block title %}Address{% endblock %}

{% block body %}
    <h1>Address</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ address.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ address.title }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ address.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ address.lastName }}</td>
            </tr>
            <tr>
                <th>AddressFirstLine</th>
                <td>{{ address.addressFirstLine }}</td>
            </tr>
            <tr>
                <th>AddressSecondLine</th>
                <td>{{ address.addressSecondLine }}</td>
            </tr>
            <tr>
                <th>AddressPostal</th>
                <td>{{ address.addressPostal }}</td>
            </tr>
            <tr>
                <th>AddressTown</th>
                <td>{{ address.addressTown }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_address_index') }}">back to list</a>

    <a href="{{ path('app_address_edit', {'id': address.id}) }}">edit</a>

    {{ include('address/_delete_form.html.twig') }}
{% endblock %}
