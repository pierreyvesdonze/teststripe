{% extends 'base.html.twig' %}

{% block title %}Discount{% endblock %}

{% block body %}
    <h1>Discount</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ discount.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ discount.code }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ discount.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_discount_index') }}">back to list</a>

    <a href="{{ path('app_admin_discount_edit', {'id': discount.id}) }}">edit</a>

    {{ include('admin_discount/_delete_form.html.twig') }}
{% endblock %}
