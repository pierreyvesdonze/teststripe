<form method="post" action="{{ path('admin_discount_delete', {'id': discount.id}) }}" onsubmit="return confirm('Êtes-vous sûr(e) de vouloir supprimer ce code promo ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ discount.id) }}">
    <button class="btn custom-btn">Supprimer</button>
</form>
